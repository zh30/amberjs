# Docker Compose for Amber Enterprise
# This file demonstrates a production-ready Amber deployment

version: '3.8'

services:
  # ==========================================================================
  # Amber Runtime Cluster
  # ==========================================================================
  amberjs-primary:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: amberjs:0.2.0
    container_name: amberjs-primary
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - AMBER_ENV=production
      - AMBER_LOG_LEVEL=info
      - AMBER_MAX_WORKERS=auto
      - AMBER_PRIMARY=true
    volumes:
      - amberjs_data:/app/data
      - amberjs_logs:/app/logs
      - ./configs:/app/configs:ro
    networks:
      - amberjs-network
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
        window: 120s
    healthcheck:
      test: ["CMD", "amber", "--health-check"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  amberjs-replica-1:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: amberjs:0.2.0
    container_name: amberjs-replica-1
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - AMBER_ENV=production
      - AMBER_LOG_LEVEL=info
      - AMBER_MAX_WORKERS=auto
      - AMBER_PRIMARY=false
      - AMBER_PRIMARY_ENDPOINT=amberjs-primary:3000
    volumes:
      - amberjs_data:/app/data
      - amberjs_logs:/app/logs
      - ./configs:/app/configs:ro
    networks:
      - amberjs-network
    depends_on:
      - amberjs-primary
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G
    healthcheck:
      test: ["CMD", "amber", "--health-check"]
      interval: 30s
      timeout: 10s
      retries: 3

  amberjs-replica-2:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: amberjs:0.2.0
    container_name: amberjs-replica-2
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      - AMBER_ENV=production
      - AMBER_LOG_LEVEL=info
      - AMBER_MAX_WORKERS=auto
      - AMBER_PRIMARY=false
      - AMBER_PRIMARY_ENDPOINT=amberjs-primary:3000
    volumes:
      - amberjs_data:/app/data
      - amberjs_logs:/app/logs
      - ./configs:/app/configs:ro
    networks:
      - amberjs-network
    depends_on:
      - amberjs-primary
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G
    healthcheck:
      test: ["CMD", "amber", "--health-check"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ==========================================================================
  # Load Balancer (HAProxy)
  # ==========================================================================
  haproxy:
    image: haproxy:2.8
    container_name: amberjs-haproxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "8404:8404"  # HAProxy stats
    volumes:
      - ./configs/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
    networks:
      - amberjs-network
    depends_on:
      - amberjs-primary
      - amberjs-replica-1
      - amberjs-replica-2

  # ==========================================================================
  # Monitoring Stack
  # ==========================================================================
  prometheus:
    image: prom/prometheus:v2.47
    container_name: amberjs-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./configs/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    networks:
      - amberjs-network

  grafana:
    image: grafana/grafana:10.1
    container_name: amberjs-grafana
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana
      - ./configs/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./configs/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    networks:
      - amberjs-network
    depends_on:
      - prometheus

  # ==========================================================================
  # Logging (ELK Stack)
  # ==========================================================================
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.10
    container_name: amberjs-elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    networks:
      - amberjs-network

  logstash:
    image: docker.elastic.co/logstash/logstash:8.10
    container_name: amberjs-logstash
    volumes:
      - ./configs/logstash:/usr/share/logstash/pipeline:ro
      - amberjs_logs:/var/log/amberjs:ro
    ports:
      - "5044:5044"
      - "9600:9600"
    environment:
      LS_JAVA_OPTS: "-Xmx256m -Xms256m"
    networks:
      - amberjs-network
    depends_on:
      - elasticsearch

  kibana:
    image: docker.elastic.co/kibana/kibana:8.10
    container_name: amberjs-kibana
    ports:
      - "5601:5601"
    environment:
      ELASTICSEARCH_HOSTS: http://elasticsearch:9200
    networks:
      - amberjs-network
    depends_on:
      - elasticsearch

  # ==========================================================================
  # Distributed Tracing (Jaeger)
  # ==========================================================================
  jaeger:
    image: jaegertracing/all-in-one:1.50
    container_name: amberjs-jaeger
    ports:
      - "16686:16686"
      - "14268:14268"
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    networks:
      - amberjs-network

  # ==========================================================================
  # Database (Optional)
  # ==========================================================================
  postgres:
    image: postgres:15
    container_name: amberjs-postgres
    environment:
      - POSTGRES_DB=amberjs
      - POSTGRES_USER=amberjs
      - POSTGRES_PASSWORD=amberjs_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - amberjs-network
    restart: unless-stopped

  # ==========================================================================
  # Redis (for caching and session storage)
  # ==========================================================================
  redis:
    image: redis:7.2-alpine
    container_name: amberjs-redis
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - amberjs-network
    restart: unless-stopped

# ==========================================================================
# Volumes
# ==========================================================================
volumes:
  amberjs_data:
    driver: local
  amberjs_logs:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
  elasticsearch_data:
    driver: local
  redis_data:
    driver: local
  postgres_data:
    driver: local

# ==========================================================================
# Networks
# ==========================================================================
networks:
  amberjs-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

# ==========================================================================
# Usage Instructions
# ==========================================================================
# 1. Start the stack:
#    docker-compose up -d
#
# 2. Access the services:
#    - Amber: http://localhost:3000
#    - Grafana: http://localhost:3001 (admin/admin)
#    - Prometheus: http://localhost:9090
#    - Kibana: http://localhost:5601
#    - Jaeger: http://localhost:16686
#
# 3. Scale Amber instances:
#    docker-compose up -d --scale amberjs-replica-1=3
#
# 4. View logs:
#    docker-compose logs -f amberjs-primary
#
# 5. Stop the stack:
#    docker-compose down
#
# 6. Clean up (including volumes):
#    docker-compose down -v

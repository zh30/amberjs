{
  "name": "amberjs-debugger",
  "displayName": "Amber Debugger",
  "description": "Debug Amber JavaScript/TypeScript runtime",
  "version": "0.1.0",
  "publisher": "amberjs-team",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Debuggers"
  ],
  "activationEvents": [
    "onCommand:amberjs.startDebugging",
    "onCommand:amberjs.stopDebugging"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "amberjs.startDebugging",
        "title": "Start Amber Debugging"
      },
      {
        "command": "amberjs.stopDebugging",
        "title": "Stop Amber Debugging"
      }
    ],
    "debuggers": [
      {
        "type": "amberjs",
        "label": "Amber",
        "program": "./dist/debugAdapter.js",
        "args": [],
        "configurationAttributes": {
          "launch": {
            "required": ["program"],
            "properties": {
              "program": {
                "type": "string",
                "description": "Path to JavaScript/TypeScript file"
              },
              "stopOnEntry": {
                "type": "boolean",
                "default": false,
                "description": "Automatically stop after launch"
              }
            }
          }
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0"
  }
}

# Amber Docker Compose Configuration
# This compose file provides a complete Amber development environment

version: '3.8'

services:
  # Amber Runtime Service
  amberjs-runtime:
    build:
      context: .
      target: development
      dockerfile: tools/ci-cd-integrations/docker/Dockerfile
    image: amberjs/runtime:dev
    container_name: amberjs-dev
    volumes:
      - .:/app
      - amberjs-cache:/app/node_modules
    ports:
      - "3000:3000"  # Application port
      - "9229:9229"  # Debug port
    environment:
      - AMBER_ENV=development
      - AMBER_DEBUG=true
      - NODE_ENV=development
    command: >
      sh -c "
        echo '🐝 Starting Amber development server...' &&
        amber run --watch src/index.js
      "
    networks:
      - amberjs-network
    restart: unless-stopped

  # Test Runner Service
  amberjs-test:
    build:
      context: .
      target: test
      dockerfile: tools/ci-cd-integrations/docker/Dockerfile
    image: amberjs/runtime:test
    container_name: amberjs-test
    volumes:
      - .:/app
      - amberjs-cache:/app/node_modules
      - test-results:/tmp
    environment:
      - AMBER_ENV=test
      - NODE_ENV=test
    command: >
      sh -c "
        echo '🧪 Running Amber tests...' &&
        amber test --reporter=json --output=/tmp/test-results.json &&
        cat /tmp/test-results.json
      "
    networks:
      - amberjs-network
    depends_on:
      - redis
      - postgres

  # Build Service
  amberjs-build:
    build:
      context: .
      target: build
      dockerfile: tools/ci-cd-integrations/docker/Dockerfile
    image: amberjs/runtime:build
    container_name: amberjs-build
    volumes:
      - .:/app
      - amberjs-cache:/app/node_modules
      - build-artifacts:/app/dist
    environment:
      - AMBER_ENV=production
      - NODE_ENV=production
    command: >
      sh -c "
        echo '🔨 Building application...' &&
        amber bundle src/index.js --outfile=dist/bundle.js --minify &&
        echo '✅ Build complete' &&
        ls -lah dist/
      "
    networks:
      - amberjs-network

  # Production Service
  amberjs-prod:
    build:
      context: .
      target: production
      dockerfile: tools/ci-cd-integrations/docker/Dockerfile
    image: amberjs/runtime:latest
    container_name: amberjs-prod
    ports:
      - "3000:3000"
    environment:
      - AMBER_ENV=production
      - NODE_ENV=production
      - PORT=3000
    command: ["amber", "start"]
    networks:
      - amberjs-network
    restart: always
    healthcheck:
      test: ["CMD", "amber", "--version"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Supporting Services
  redis:
    image: redis:7-alpine
    container_name: amberjs-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - amberjs-network
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    container_name: amberjs-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=amberjs
      - POSTGRES_USER=amberjs
      - POSTGRES_PASSWORD=amberjs123
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - amberjs-network
    restart: unless-stopped

  # Monitoring
  prometheus:
    image: prom/prometheus:latest
    container_name: amberjs-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./tools/ci-cd-integrations/docker/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    networks:
      - amberjs-network
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: amberjs-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=amberjs123
    volumes:
      - grafana-data:/var/lib/grafana
    networks:
      - amberjs-network
    restart: unless-stopped

# Volumes
volumes:
  amberjs-cache:
    driver: local
  test-results:
    driver: local
  build-artifacts:
    driver: local
  redis-data:
    driver: local
  postgres-data:
    driver: local
  prometheus-data:
    driver: local
  grafana-data:
    driver: local

# Networks
networks:
  amberjs-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
